# Optional — only needed when DRY_RUN=false / DATABASE_URL is set.
# Demo mode needs none of these (pure stdlib), but Vercel installs them so you can
# flip to live without redeploying config.
httpx==0.27.2
anthropic==0.39.0
openai==1.51.0
psycopg[binary]==3.2.3
pillow==10.4.0
